#include <bits/stdc++.h>
#include <boost/multiprecision/cpp_int.hpp>
using namespace std;
using boost::multiprecision::cpp_int;
cpp_int fact(cpp_int n)
    {
        if(n<=1)
        {return 1;}
        else
        {
           return n*fact(n-1);
        }
    }
    int main()
    {
    cpp_int t,p;
    cin>>t;
     while(t>0)
     {
         cin>>p;
         cout<<fact(p)<<endl;
         t=t-1;
     }
     //cout<<count;
    return 0;
    } 
